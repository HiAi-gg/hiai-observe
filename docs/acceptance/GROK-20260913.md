# HIAI-OBSERVE acceptance — 2026-09-13

In-place checkout. No git, publish, Docker daemon, shared-DB migrate, or secret reads.
Baseline HEAD: `5eb0ce745586465f6575e608c6bdbc2e43c7775b` (detached). Owner-dirty at start: `docs/ROADMAP.md` modified; `ROADMAP.md`, `TEAM_BACKLOG.md`, `docs/PLAN.md` untracked — preserved.

## This pass (local queue continuation)

Independently inspected prior Grok diffs and coordinator/hiai-observe-review.md. Closed local blockers so unit tests, typecheck, build, and database-gated integration can run without touching shared workstation data (`app_hiai_observe` / role `app_hiai_observe`).

### Isolation

- Live inserts require `TENANT_SCOPE_LIVE_DB=1` **and** `isIsolatedObserveFixture`.
- GitHub Actions: `observe@localhost|127.0.0.1/hiai_observe` only when `GITHUB_ACTIONS=true`.
- Local: `observe_test@127.0.0.1|localhost/hiai_observe_test` only.
- `app_hiai_observe` is never a live fixture.
- Default `bun run test` excludes `tests/e2e/**` and `tests/integration/**` unless `INTEGRATION=1`.
- Those helpers lazy-import `store/db.js`. INTEGRATION also requires the isolated DSN.
- Test `NODE_ENV` without `DATABASE_URL` uses unreachable `127.0.0.1:1` / `hiai_observe_test` (not the shared default). Pool-stat timers do not start in test.
- `tenant-scope.test.ts` has no top-level db/plugin import; skipIf runs first.

Created disposable Postgres database `hiai_observe_test` owned by role `observe_test`. Did **not** migrate, seed, or query `app_hiai_observe`.

### 0003 old→new (disposable only)

`OBSERVE_MIGRATE_LIVE=1` + isolated DSN. Applies 0000→0002, inserts a log row, applies 0003, asserts nullable `trace_id`/`span_id`, old row remains readable with `raw.traceId`, new insert writes first-class columns. Drops public **tables** only (not schema/extensions). CI Test job runs this file **before** `scripts/migrate.ts`.

### T01 / T02 / T03

| Task | Status | Notes |
|------|--------|--------|
| T01 staff UI 5197 / tenant bridge | **review / partial** | Source config + mocked applyScope. Vite not started. Empty-key sentinel in vite source. |
| T02 log↔trace + migration | **review** | Schema distinct; 0003 additive SQL; isolated old→new **passed** on `hiai_observe_test`. No 0002/0003 drizzle snapshots (historical). |
| T03 health / gates | **review** | Public `/health` ≡ `/api/health`. Live tenant 3/3 on disposable fixture. Docker Hub still waits for CI; SHA tag `workflow_run.head_sha`. |

## Focused checks (this pass)

All via `python3 /mnt/data/work/grok-portfolio-20260913/inplace/run-check.py` from `/mnt/data/projects/hiai-observe`. `PATH` includes `/home/vlgalib/.bun/bin`.

| Command | Exit | Counts |
|---------|------|--------|
| `bun --no-env-file run test tests/lib/isolated-db.test.ts tests/store/connection-string.test.ts tests/lib/unit-suite-imports.test.ts tests/middleware/tenant-scope-resolve.test.ts` | **0** | 4 files, **24 passed** |
| `OBSERVE_MIGRATE_LIVE=1 DATABASE_URL=postgresql://observe_test@127.0.0.1:5432/hiai_observe_test bun --no-env-file run test tests/migrations/0003-apply.test.ts` | **0** | 1 file, **1 passed** (old→new) |
| `TENANT_SCOPE_LIVE_DB=1 DATABASE_URL=…hiai_observe_test bun --no-env-file run test tests/middleware/tenant-scope.test.ts` | **0** | 1 file, **3 passed** (live insert/delete on fixture) |
| `env -u DATABASE_URL -u TENANT_SCOPE_LIVE_DB -u OBSERVE_MIGRATE_LIVE -u INTEGRATION bun --no-env-file run test` | **0** | 65 files passed / 2 skipped; **619 passed / 4 skipped** |
| `bun --no-env-file run typecheck` | **0** | `tsc --noEmit` clean |
| `bun --no-env-file run build` | **0** | 635 modules, `dist/index.js` 2.27 MB |
| `bun --no-env-file run --cwd frontend test` | **0** | 3 files, **154 passed** |

INTEGRATION e2e against a booted `:8001` **not run** (would need a running API). No Docker Hub / npm / git. No SSH.

## Remaining

- T01 runtime Vite `:5197` `/hiai-observe` and Caddy LAN path.
- INTEGRATION e2e/otlp/sentry against a booted server on the disposable fixture (not `app_hiai_observe`).
- Drizzle `0002`/`0003` snapshots still absent (handwritten SQL).
- Coolify/prod `/api/health`, Docker Hub publication, npm publish: not this pass.
- Wave leftovers: PM-INF-1, PM-RBAC.
- No commit/push.
