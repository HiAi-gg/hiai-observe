# hiai-observe recovery — 2026-09-14

In-place checkout `/mnt/data/projects/hiai-observe`. No git commit/push, no Docker Hub/npm publish, no Coolify/production source edits, no DNS, no destructive seed, no shared-service daemons, no worktrees/copies.

Baseline HEAD: `5eb0ce745586465f6575e608c6bdbc2e43c7775b` (detached). Exclusive project: **hiai-observe** (HiAI core → **INFRA-01**). RUS sites (somebar, cucoj_ru, withusbetter) and other portfolio jobs were not touched.

`docs/workstreams/20260914` and `OWNER_BRIEF` are **not present** in this repo or under `/mnt/data/projects/docs`. Prior `docs/acceptance/GROK-20260913.md` + coordinator `hiai-observe-review.md` were read; T02/T03 research was not redone.

## Process / session gate

| Item | Finding |
|------|---------|
| This writer | grok PID **170531**, session `01a0a177-0e9d-7801-baa1-bd72caf0db4f`, cwd hiai-observe |
| Other Grok on this repo | none (TUI grok cwds: docsmint, goalsplanning, hiai_agents) |
| Existing processes | `bun run --parallel dev:api dev:ui` (API `:8001`, Vite `:5197`) — readers, not editors |
| `.git/index.lock` | absent |
| Dirty tree | preserved; no reset/stash |

## Independent T01 re-review (was partial)

| Check | Result |
|-------|--------|
| Source: `frontend/vite.config.ts` port 5197, `LAN_UI_BASE=/hiai-observe`, `hiaiApiKey=""` | pass (`tests/staff-ui-config.test.ts`) |
| Live `GET http://127.0.0.1:5197/hiai-observe/` | **HTTP 200**, `theme-observe`, `PUBLIC_BASE_PATH=/hiai-observe` |
| Compiled `stores.svelte.ts` via Vite | placeholder replaced with empty `""`; no `ho_` key |
| `scripts/vite-health-gate.ts` | **ok** status=200 bytes=1777 |
| `VITE_HEALTH_GATE=1` runtime test | **2 passed** |
| Unauthenticated `GET /api/dashboard` on `:8001` | **401** `{"error":"Unauthorized"}` |
| Same via Vite proxy `/hiai-observe/api/dashboard` | **401** |
| `http://hiai-observe.localhost/` (Caddy → `:8001`) | **200** |
| `http://127.0.0.1/hiai-observe/` | **502** — Caddy Host `127.0.0.1` dials `:50111` (not this app). Intended Windows LAN is `https://192.168.1.111/hiai-observe/` (`zz-lan-portfolio.caddy`); `192.168.1.111:80` is not reachable from this host. Shared Caddy was **not** edited. |
| Lightpanda | see below |

T01 is **review** with runtime evidence on loopback `:5197`. Remaining: Windows LAN HTTPS path not probed from DEV-01.

## This pass (implementation)

1. **Restore DB tenant tests to CI** — Test job already had `TENANT_SCOPE_LIVE_DB=1`. Added explicit step `Live tenant-scope DB tests` (`bunx vitest run tests/middleware/tenant-scope.test.ts`) **after** migrate. E2E job still includes the same file. Live inserts still require isolated DSN (`GITHUB_ACTIONS` `observe@localhost/hiai_observe` or local `observe_test`/`hiai_observe_test`). Shared `app_hiai_observe` rejected.
2. **Start Vite health gate** — `scripts/vite-health-gate.ts` + CI job `vite-health` (install frontend, `bun run dev` on 5197, probe `/hiai-observe/`). Local gate used the already-running Vite (`strictPort`; did not start a second server). `package.json` script `vite:health`.
3. **Do not publish image** — `docker.yml` still pushes only on successful CI `workflow_run` from push. This pass did not trigger Hub, tags, or Coolify.
4. Closed prior dirty-test gaps so the unit suite is green:
   - `resetOperationalCountersForTests` / OTLP + retention Prometheus counters in `src/middleware/metrics.ts`, wired from `src/api/otlp.ts` and `src/workers/retention.ts`.
   - Retention `TABLE_DEFS` includes `gpu_stats` (`collected_at`); `batchDelete` prefers `result.count`.
   - Esbuild-breaking `*/` in `tests/middleware/tenant-scope-resolve.test.ts` comment.

## Files this pass added or edited

```
.github/workflows/ci.yml
CHANGELOG.md
package.json
scripts/vite-health-gate.ts
src/api/otlp.ts
src/middleware/metrics.ts
src/workers/retention.ts
tests/middleware/tenant-scope-resolve.test.ts
tests/staff-ui-config.test.ts
tests/staff-ui-runtime.test.ts
TEAM_BACKLOG.md
GROK_RESULT.json
docs/acceptance/RECOVERY-20260914.md
```

Pre-existing dirty/untracked files from 2026-09-13 (ROADMAP, T02 0003 SQL, isolation helpers, docker.yml SHA gate, etc.) were **left in place**. Full porcelain at end of pass: 42 modified tracked files + untracked listed below. `dist/index.js` is a build artifact from `bun run build`.

## Test evidence (run-check.py slots)

All via `python3 /mnt/data/work/grok-portfolio-20260913/inplace/run-check.py` from repo root. `PATH` includes `/home/vlgalib/.bun/bin`.

| Command | Exit | Counts |
|---------|------|--------|
| `bun --no-env-file run test tests/staff-ui-config.test.ts tests/staff-ui-runtime.test.ts tests/lib/isolated-db.test.ts tests/lib/unit-suite-imports.test.ts tests/middleware/tenant-scope-resolve.test.ts` | **0** | 5 files, **29 passed / 1 skipped** |
| `VITE_HEALTH_GATE=1 bun --no-env-file run test tests/staff-ui-runtime.test.ts` | **0** | **2 passed** |
| `bun scripts/vite-health-gate.ts` | **0** | `ok http://127.0.0.1:5197/hiai-observe/ status=200` |
| `env -u DATABASE_URL -u TENANT_SCOPE_LIVE_DB -u OBSERVE_MIGRATE_LIVE -u INTEGRATION -u VITE_HEALTH_GATE bun --no-env-file run test` | **0** | 69 files passed / 2 skipped; **639 passed / 5 skipped** (e2e/integration excluded; live tenant + 0003-apply skipped without opt-in) |
| `bun --no-env-file run typecheck` | **0** | `tsc --noEmit` clean |
| `bun --no-env-file run build` | **0** | 636 modules, `dist/index.js` 2.27 MB |
| `bun --no-env-file run --cwd frontend test` | **0** | 3 files, **154 passed** |

Live tenant 3/3 on `observe_test` **not re-run this pass**: no `.pgpass`, peer auth failed for `observe_test`, TCP required a password that was not read. Prior 2026-09-13 evidence on disposable `hiai_observe_test` stands. CI source now fails hard when `GITHUB_ACTIONS=true` + isolated service DSN.

INTEGRATION e2e/otlp/sentry against a booted `:8001` **not run** (would need disposable fixture, not `app_hiai_observe`).

## Domain candidates (ownership not assumed)

hiai-observe is **INFRA-01** Coolify service (`app-2qjewz…` `:8001` per `.devstack/WHERE.md`). Not a public brand site. **Do not** treat `hiai.gg` / `observe.hiai.gg` / similar as owned.

| Candidate | Role | This pass |
|-----------|------|-----------|
| `http://hiai-observe.localhost` / `http://api.hiai-observe.localhost` | local Caddy vhosts → `:8001` | HTTP 200 health; no DNS change |
| `http://127.0.0.1:5197/hiai-observe/` | staff UI Vite | verified |
| `https://192.168.1.111/hiai-observe/` | Windows LAN (Caddy snippet) | not reachable from this host; coordinator/Caddy, not this repo |
| INFRA-01 Coolify `hiai-observe` | production | **untouched**; preserve live |

DNS of docsmint.com / newmap.online / goalsplanning.com is frozen. No DNS edits.

## Deployment steps (controller only — not executed)

1. Independent review of this doc + diff. Do **not** merge from detached HEAD without choosing the existing branch (`release/2026-09-06-hiai-observe` is at the same SHA).
2. Isolated 0003 old→new already has a CI step (`OBSERVE_MIGRATE_LIVE=1` before `scripts/migrate.ts`). Do **not** apply 0003 to `app_hiai_observe` or INFRA-01 until that review.
3. After acceptance: PR/CI. Required jobs include Test (tenant step), E2E, `vite-health`, frontend tests. Docker **CI** job builds a local image only.
4. Hub publication (`docker.yml`) waits for successful CI `workflow_run` on **push**. `latest` only if `workflow_run.head_sha` is still `origin/main`. Do not `workflow_dispatch` a push. This preparation pass publishes nothing.
5. Coolify/INFRA-01 rollout is a later controller decision. Production compose healthcheck is `GET /api/health`. Entrypoint still runs `scripts/migrate.ts` on container start — 0003 is therefore **not** optional once an image that contains it is deployed.
6. DNS coordinator owns DNS. No record changes for this service in this pass.

## Blockers

- No commit/push/deploy (this pass by design).
- 0003 not applied to shared `app_hiai_observe` or production. No 0002/0003 drizzle snapshots (handwritten SQL; journal test locks that).
- Local live tenant / 0003 apply need `observe_test` credentials; not fetched (no secrets printed).
- INTEGRATION e2e not run.
- Windows LAN `https://192.168.1.111/hiai-observe/` unverified; `http://127.0.0.1/hiai-observe/` 502 is a shared Caddy host-match, not a Vite bind failure.
- Wave leftovers: PM-INF-1, PM-RBAC.
- Detached HEAD; do not invent a main checkout or force-push.

## Lightpanda (Linux)

Binary: `/home/vlgalib/.local/bin/lightpanda` version **1.0.0-nightly.8779+23528f0cc**.

| Mode | Result |
|------|--------|
| `fetch --dump markdown --json http://127.0.0.1:5197/hiai-observe/` | HTTP 200; executed enough client JS to render nav under `/hiai-observe/`, Dashboard heading, and **Error loading dashboard: Unauthorized** |
| `fetch --dump html` | HTTP 200; inlines large Tailwind CSS; piping stdout caused `WriteFailed` (dump size) |

Rendering limitations vs Chromium: no screenshot/visual layout, no mobile viewport matrix, incomplete CSS/JS compared with Chrome, no hover/click automation used here. Semantic markdown dump was enough to confirm staff shell + unauthorized API. agent-browser/Chromium was not required after Lightpanda produced that dump.

## Untracked (whole tree, including prior)

`GROK_RESULT.json`, `ROADMAP.md`, `TEAM_BACKLOG.md`, `docs/PLAN.md`, `docs/acceptance/GROK-20260913.md`, `docs/acceptance/RECOVERY-20260914.md`, `drizzle/0003_logs_trace_correlation.sql`, `scripts/vite-health-gate.ts`, `src/lib/redact.ts`, `src/store/connection-string.ts`, `tests/ingestion/otlp-logs.test.ts`, `tests/lib/apply-scope.test.ts`, `tests/lib/isolated-db.test.ts`, `tests/lib/isolated-db.ts`, `tests/lib/redact.test.ts`, `tests/lib/unit-suite-imports.test.ts`, `tests/middleware/metrics-ingest.test.ts`, `tests/middleware/tenant-scope-resolve.test.ts`, `tests/migrations/0003-apply.test.ts`, `tests/migrations/journal.test.ts`, `tests/staff-ui-config.test.ts`, `tests/staff-ui-runtime.test.ts`, `tests/store/connection-string.test.ts`, `tests/store/logs-redact.test.ts`, `tests/store/schema-correlation.test.ts`.
